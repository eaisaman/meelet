require.config(
    {
        paths: {
            "underscore": UNDERSCORE_LIB_PATH + "underscore-min"
        }
    }
);

define(
    [
            "underscore"
    ],
    function () {
        return function () {
        }
    }
);